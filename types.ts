import { Order, Picker, Item, ItemDetails, Cart, PickerLevel } from '@prisma/client';

export type IOrder = Order;
export type IPicker = Picker;
export type IItem = Item;
export type IItemDetails = ItemDetails;
export type ICart = Cart;

export type IPickerLevel = PickerLevel;
